package jabberpoint.command;

// ISP: One method interface - classes only implement what they need
// Command pattern: defines the contract for all commands
public interface Command {
    void execute();
}